# HillCypher-Encryption

Encrypts plaintext using the Hill cipher in C. Reads an n×n key matrix from file, filters alphabetic characters, converts to lowercase, pads with `x`, then performs modulo-26 matrix encryption and outputs ciphertext in 80-character lines.

---

## 📌 Resume Entry (copy to project section)

**Hill Cipher Encryption — C (CIS3360)**  
• Implemented Hill Cipher encryption using matrix multiplication + modulo-26 arithmetic  
• Read key + plaintext via CLI, normalized text and padded final block  
• Dynamically allocated N×N matrix (2–9) and formatted ciphertext output  
